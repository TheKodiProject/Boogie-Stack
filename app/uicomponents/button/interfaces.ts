import { ReactNode } from 'react'

export interface IBoogieButton {
  // Label - text to be displayed on the button
  text: string
  // Icon - react node to be displayed on the button
  icon?: ReactNode
}
